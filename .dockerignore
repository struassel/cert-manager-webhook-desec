# 1. Ignore everything
*

# 2. Re-include the files you want to keep
!go.mod
!go.sum
!**/*.go

# 3. Re-include directories (so Docker can traverse them to find .go files)
# This is the magic part! It un-ignores directories, but not the files *inside* them.
!desec/